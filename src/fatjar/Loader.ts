// src/fatjar/Loader.ts

export class Loader {
    static loadResource(name: string): string | Buffer | undefined {
        // In Node.js, use fs.readFileSync or import.meta.url
        return undefined; // Placeholder
    }
}
